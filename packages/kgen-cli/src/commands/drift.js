/**
 * Drift Command Group
 * 
 * Commands for artifact drift detection and analysis.
 * Re-exports the existing drift/index.js command structure.
 */

import driftCommands from './drift/index.js';

export default driftCommands;
