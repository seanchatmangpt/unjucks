/**
 * Validate Command Group
 * 
 * Commands for comprehensive validation of KGEN artifacts, graphs, and configurations.
 * Re-exports the existing validate/index.js command structure.
 */

import validateCommands from './validate/index.js';

export default validateCommands;
