export default defineNuxtConfig({
  devtools: { enabled: true },
  modules: [
    // MODULES
@nuxtjs/tailwindcss
  ],
  css: [
    // CSS FILES
  ],
  plugins: [
    // PLUGINS
  ],
  build: {
    // BUILD CONFIG
  }
});