export default defineNuxtConfig({
  devtools: { enabled: true },
  modules: [
    // MODULES
  ],
  css: [
    // CSS FILES
  ],
  plugins: [
    // PLUGINS
  ],
  build: {
    // BUILD CONFIG
  }
});