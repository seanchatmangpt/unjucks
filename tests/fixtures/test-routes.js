// Test file for injection
const config = {
  name: 'test',
  // INSERT_ROUTES_HERE
  routes: [],
};

// IMPORT_SECTION
import express from 'express';
import express from 'express';

module.exports = config;
