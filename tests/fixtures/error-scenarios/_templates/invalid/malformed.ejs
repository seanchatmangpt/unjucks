---
to: src/invalid/{{ fileName }}.ts
variables:
  fileName:
    type: string
    required: true
# Missing closing quote on description - intentional error
    description: Malformed frontmatter for error testing
  invalidVar:
    type: unknown_type
    default: {{ unclosed_expression
---
// This template has intentional errors for testing error handling
export const {{ fileName | pascalCase }} = {
  // Missing closing brace for testing syntax errors
  invalid: true
  // Unclosed object
};